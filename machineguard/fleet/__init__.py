"""Fleet fingerprinting module.

Uses learned embeddings to map machines into a shared latent space,
enabling cross-machine similarity detection and early warning alerts
based on degradation patterns seen in other machines.
"""
